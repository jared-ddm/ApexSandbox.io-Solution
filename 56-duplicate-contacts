public Boolean duplicateContacts(Contact c1, Contact c2) {
    if (c1.Email == c2.Email && (c1.Email != null || c2.Email != null)) {
        return true;
    } else if (c1.Phone == c2.Phone && (c2.Phone != null || c2.Phone != null)) {
        return true;
    } else {
        return false;
    }
}