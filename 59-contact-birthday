public Boolean isBirthday(Contact c) {
    if (System.today().month() == c.Birthdate.month() && System.today().day() == c.Birthdate.day()) {
        return true;
    } else {
        return false;
    }
}